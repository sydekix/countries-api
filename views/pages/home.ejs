<%- include('../partials/header') %>
    <main class="main container-fluid">
        <div class="country-spacing text-center">
            <h1><%= data[0].name %></h1>
            <img src="<%= data[0].media.flag %>" alt="<%= data[0].name %>">
        </div>
    </main>

<%- include('../partials/footer') %>